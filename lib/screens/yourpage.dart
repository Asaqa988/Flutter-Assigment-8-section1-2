import 'package:flutter/material.dart';

class your_page extends StatelessWidget {
  const your_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
